/target
web/node_modules/
web/dist/

# Artefacts de release (générés par scripts/release.sh)
dist-release/
rdp-sidecar/target/
# Les paquets IronRDP portés se compilent aussi seuls (verifier-portes.sh).
rdp-sidecar/vendor/*/target/
test-rdp-server/target/
test-rdp-server/*.pem
test-vnc-server/target/
crates/avash-ui/binaries/
e2e/node_modules/

# node_modules (tous emplacements) + binaires de test
node_modules/
crates/avash/avash-test
avash-test
web/public/hero.png

# Extraction d'une AppImage (squashfs-root) : artefact local, jamais versionné
squashfs-root/

# Captures de régression visuelle produites localement (les références sont dans e2e/visuel/reference)
e2e/.tmp/
# Résultat des mesures du front (scripts/mesures-front.sh) : les chiffres
# retenus sont reportés dans docs/feuille-de-route.md
e2e/mesures/resultat.json

# Schémas générés par Tauri à chaque construction (autocomplétion des
# capacités) : leur contenu suit les fonctionnalités compilées, ils ne se
# commitent pas — le modèle officiel les ignore aussi.
crates/avash-ui/gen/schemas/

# Construction Flatpak locale (packaging/flathub)
build-flatpak/
.flatpak-builder/
